// index.js
// 获取应用实例
import api from '../../api/api.js'
import wxApi from '../../utils/wxApi.js'
import { http } from '../../utils/http.js'
const app = getApp()

Page({
  data: {
  },
  onLoad: function () {
  }
})
